name: Build
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: setup dotnet
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5.4.0
        with:
          dotnet-version: '10.0.x'
      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@30375c66a4eea26614e0d39710365f22f8b0af57 # v3.0.0
        with:
          msbuild-architecture: x64
      - run: "nuget restore ${env:GITHUB_WORKSPACE}\\cs-windows-firewall-bouncer.sln"
      - name: Build
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          msbuild ${env:GITHUB_WORKSPACE}\cs-windows-firewall-bouncer.sln '-t:cs-windows-firewall-bouncer:rebuild;cs-windows-firewall-bouncer-tests:rebuild' -property:Configuration=Release
          if ($LASTEXITCODE -ne 0) { throw "msbuild failed with exit code $LASTEXITCODE" }
      - name: Test
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          dotnet test ${env:GITHUB_WORKSPACE}\cs-windows-firewall-bouncer-tests\cs-windows-firewall-bouncer-tests.csproj --no-build --no-restore -c Release -p:Platform=x64 --logger "console;verbosity=normal"
          if ($LASTEXITCODE -ne 0) { throw "dotnet test failed with exit code $LASTEXITCODE" }
